app
		.controller(
				"pedidoController",
				function($scope, httpservice, $sessionStorage) {

					$scope.total = 0;
					
					
					/**
					 * funcion de inicializacion
					 */
					(function() {
						// cargar los productos.
						
						//refrescarProds();
						// inicializar la variable del storage que tiene lo
						// elegido.

						//$sessionStorage.items = [];

					}());

					/**
					 * Funcion para agregar un item al carrito.
					 */
					
					
					
					
					
				});